---
description: 使用计划模板执行实施规划工作流程以生成设计工件
---

## 配置读取

在执行此命令之前，必须先从 `.specify/config.json` 读取以下配置：
- `feature_directory`: 功能目录路径
- `templates_directory`: 模板目录路径  
- `memory_directory`: 内存目录路径
- `commands_directory`: 命令目录路径

后续所有路径引用都使用这些配置变量。

## 用户输入

```text
$ARGUMENTS
```

在继续之前，你**必须**考虑用户输入（如果不为空）。

## 概述

1. **设置**：确定当前功能的目录和可用文档列表。

2. **加载上下文**：读取功能规范和 `{memory_directory}/constitution.md`。加载实施计划模板。

3. **执行计划工作流程**：按照实施计划模板中的结构：
   - 填写技术上下文（将未知项标记为 "NEEDS CLARIFICATION"）
   - 从章程填写章程检查部分
   - 评估门控（如果违规无法证明则错误）
   - 阶段 0：生成 research.md（解决所有 NEEDS CLARIFICATION）
   - 阶段 1：生成 data-model.md、contracts/、quickstart.md
   - 阶段 1：通过运行代理脚本更新代理上下文
   - 设计后重新评估章程检查

4. **停止并报告**：命令在阶段 2 规划后结束。报告分支、IMPL_PLAN 路径和生成的工件。

## 阶段

### 阶段 0：大纲和研究

1. **从上述技术上下文中提取未知项**：
   - 对于每个 NEEDS CLARIFICATION → 研究任务
   - 对于每个依赖项 → 最佳实践任务
   - 对于每个集成 → 模式任务

2.3. **生成并派发研究代理**：

   ```text
   对于技术上下文中的每个未知项：
     任务："为 {功能上下文} 研究 {未知项}"
   对于每个技术选择：
     任务："在 {领域} 中查找 {技术} 的最佳实践"
   ```

3. **在 `research.md` 中整合发现**，使用格式：
   - 决策：[选择了什么]
   - 理由：[为什么选择]
   - 考虑的替代方案：[还评估了什么]

**输出**：包含所有 NEEDS CLARIFICATION 已解决的 research.md

4. **生成设计工件**：使用 `{templates_directory}/` 中的模板，在功能目录创建：

### 阶段 1：设计和合约

**先决条件**：`research.md` 完成

1. **从功能规范中提取实体** → `data-model.md`：
   - 实体名称、字段、关系
   - 来自需求的验证规则
   - 如果适用的状态转换

2. **从功能需求生成 API 合约**：
   - 对于每个用户操作 → 端点
   - 使用标准 REST/GraphQL 模式
   - 将 OpenAPI/GraphQL 模式输出到 `/contracts/`

3. **代理上下文更新**：
   - 检测正在使用的 AI 代理
   - 更新相应的代理特定上下文文件
   - 仅从当前计划添加新技术
   - 在标记之间保留手动添加

**输出**：data-model.md、/contracts/*、quickstart.md、代理特定文件

## 关键规则

- 使用绝对路径
- 在门控失败或未解决的澄清时错误